// src/types/entities.ts

// Пользователь нашего приложения
export interface User {
    id: string;
    email: string;
    // ЗАДАНИЕ 1: Добавь сюда еще одно поле, например, для имени пользователя.
    // Придумай название поля сам (например, username или fullName).
    // ... твой код здесь
    username: string;
}

// Списки задач (бывшие "метки"). У задачи может быть только один такой список.
export interface List {
    id: string;
    name: string;
    // ЗАДАНИЕ 2: Каждому списку нужен "владелец".
    // Добавь сюда поле для ID пользователя, которому принадлежит этот список.
    // Подумай, как его лучше назвать (например, ownerId или userId).
    // ... твой код здесь
    userOwnerId: string;
}

// Сама задача
export interface Task {
    id: string;
    title: string;
    description?: string; // Описание может быть, а может и не быть (необязательное поле)

    // ЗАДАНИЕ 3: Заполни блок с датами.
    // Нам нужна дата начала и дата конца. Обе могут отсутствовать у задачи.
    // Тип для дат в JS - это `Date`. Поля должны быть необязательными.
    // ... твой код здесь (два поля)
    startDate?: Date;
    endDate?: Date;

    // ЗАДАНИЕ 4: Заполни блок со связями.
    // Каждая задача должна "знать", какому пользователю и какому списку она принадлежит.
    // Добавь сюда два поля: ID пользователя и ID списка.
    // ID списка может отсутствовать (для задач "без метки").
    // ... твой код здесь (два поля)
    userOwnerId: string;
    listOwnerId: string;

    // ЗАДАНИЕ 5: Задачи могут быть выполненными или избранными.
    // Добавь два булевых поля для этих состояний.
    isCompleted: boolean;
    // ... твой код для "избранного" здесь
    isFavourite: boolean;
}